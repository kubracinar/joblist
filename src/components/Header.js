import React from "react";
import { Container } from "reactstrap";

const Header = () => {

    return (
        <div>

                <Container fluid id="head">
                    <h1 className="display-5">Job List</h1>
                </Container>

        </div>
    );
};

export default Header;